'use client';

import { Menu } from 'lucide-react';

export default function Header() {
  return (
    <header className="h-[60px] border-b border-[#1A1A1A] flex items-center px-4">
      {/* Content of header can go here if needed */}
    </header>
  );
} 